---
phase: 01-chat-widget
plan: 03
type: execute
wave: 3
depends_on:
    - '01-02'
files_modified:
    - src/App.tsx
autonomous: false
requirements:
    - CHAT-01
    - CHAT-06

must_haves:
    truths:
        - 'Floating chat button is visible on the portfolio page in a browser'
        - 'Chat panel opens and closes correctly via the button and the × close button'
        - 'The widget renders without JavaScript console errors'
        - 'ChatBot renders as a sibling of Cursor — both are fixed-position elements outside <main>'
    artifacts:
        - path: 'src/App.tsx'
          provides: 'ChatBot imported and rendered alongside Cursor'
          contains: '<ChatBot />'
    key_links:
        - from: 'src/App.tsx'
          to: 'src/components/ChatBot.tsx'
          via: "import ChatBot from './components/ChatBot'"
          pattern: 'import ChatBot'
---

<objective>
Wire `<ChatBot />` into `src/App.tsx` and do a human visual verification that the full widget works in the browser.

Purpose: Plan 02 built the component in isolation. This plan connects it to the live app so the visitor can actually see and use it. The human-verify checkpoint confirms the complete CHAT-01–CHAT-10 flow works end-to-end before the phase is marked complete.

Output: `src/App.tsx` updated with `<ChatBot />` import and render. Human confirms the widget is functional.
</objective>

<execution_context>
@/home/karl/.claude/get-shit-done/workflows/execute-plan.md
@/home/karl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chat-widget/01-CONTEXT.md
@.planning/phases/01-chat-widget/01-02-SUMMARY.md

<interfaces>
<!-- What App.tsx needs to import. From Plan 02 output. -->

From src/components/ChatBot.tsx (default export):

```typescript
export default ChatBot; // React component, no props required
```

Current App.tsx render structure (from src/App.tsx):

```tsx
return (
    <>
        <Cursor />
        <Chrome theme={theme} onToggleTheme={toggleTheme} timeStr={timeStr} />
        <main>{/* page sections */}</main>
    </>
);
```

Target: Add `<ChatBot />` as a sibling of `<Cursor />` — both are fixed-position overlays outside `<main>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import and render ChatBot in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - /home/karl/Documents/repository/personal-website-v2/src/App.tsx (read current file fully before editing — confirm current import list and JSX structure)
    - /home/karl/Documents/repository/personal-website-v2/src/components/ChatBot.tsx (confirm the default export name is ChatBot)
  </read_first>
  <action>
Make two changes to `src/App.tsx`:

1. Add import after the existing `Cursor` import line:

    ```typescript
    import ChatBot from './components/ChatBot';
    ```

2. Add `<ChatBot />` as a sibling of `<Cursor />` inside the fragment, immediately after `<Cursor />`:
    ```tsx
    return (
        <>
            <Cursor />
            <ChatBot />
            <Chrome theme={theme} onToggleTheme={toggleTheme} timeStr={timeStr} />
            <main>...</main>
        </>
    );
    ```

No other changes to App.tsx. The import placement follows the existing pattern: component imports are grouped after hook imports. `ChatBot` goes after `Cursor` alphabetically and logically (both are fixed-position overlay components).

After editing, run `npx tsc --noEmit` to confirm no TypeScript errors.
</action>
<verify>
<automated>
grep 'import ChatBot' /home/karl/Documents/repository/personal-website-v2/src/App.tsx
grep '<ChatBot' /home/karl/Documents/repository/personal-website-v2/src/App.tsx
npx tsc --noEmit --project /home/karl/Documents/repository/personal-website-v2/tsconfig.json 2>&1 | head -20
</automated>
</verify>
<done> - `src/App.tsx` contains `import ChatBot from './components/ChatBot'` - `src/App.tsx` contains `<ChatBot />` in the JSX return - `npx tsc --noEmit` passes with zero errors
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — full chat widget in browser</name>
  <files>none</files>
  <action>
Human verifies the complete widget works in the browser. Run `npm run dev`, open http://localhost:5173, and follow the verification steps below. No code changes expected during this task — it is a blocking gate before phase completion.
  </action>
  <what-built>
    Complete chatbot widget: floating button (bottom-right), animated panel, prompt chips, Gemini AI integration, loading state, error handling, and design system styling. All 10 CHAT requirements are implemented.
  </what-built>
  <how-to-verify>
    1. Ensure your `.env` file has `VITE_GEMINI_API_KEY=YOUR_ACTUAL_KEY` (get from https://aistudio.google.com/app/apikey if needed)
    2. Run `npm run dev` and open http://localhost:5173
    3. Confirm the chat button is visible: fixed circle at bottom-right (`bottom-6 right-6`), uses `--color-fg` background (dark on light theme)
    4. Click the button — confirm the panel slides up with animation (~220ms), button icon changes to ×
    5. Confirm four chip buttons appear: "Who are you?", "What do you build?", "What are you working on?", "How can I reach you?"
    6. Click a chip — confirm it sends immediately (chips disappear, user bubble appears, loading dots appear, AI response arrives)
    7. Type a follow-up question in the input and press Enter — confirm it sends, loading dots appear, AI responds using prior context
    8. Test empty input — confirm send button is disabled (cursor-not-allowed, dimmed) when field is empty
    9. Click the × button in the panel header — confirm panel closes, button icon reverts to chat bubble
    10. Toggle dark mode — confirm widget adapts automatically (no hardcoded colors)
    11. Check browser console — confirm no JavaScript errors
    12. (Optional) Ask an off-topic question ("What is the capital of France?") — confirm the guard reply: "I can only answer questions about Karl. Try asking about his experience, projects, or skills."
  </how-to-verify>
  <verify>
    <automated>MISSING — this task requires human visual confirmation in a browser</automated>
  </verify>
  <done>User types "approved" after confirming all 10 CHAT requirements work in the browser.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
```bash
# App.tsx integration check
grep 'import ChatBot' src/App.tsx
grep '<ChatBot' src/App.tsx

# TypeScript clean

npx tsc --noEmit

# No hardcoded hex anywhere in the chat files

grep -rE '#[0-9a-fA-F]{3,6}' src/components/ChatBot.tsx src/constants/chatbot-context.ts && echo "FAIL" || echo "PASS"

# Build check (confirms production bundle builds correctly)

npm run build 2>&1 | tail -10

```
</verification>

<success_criteria>
1. `src/App.tsx` imports and renders `<ChatBot />` as a sibling of `<Cursor />`
2. `npx tsc --noEmit` and `npm run build` both succeed
3. Human verification confirms all 10 CHAT requirements are met in the browser:
   - CHAT-01: Floating button visible fixed bottom-right
   - CHAT-02: User message produces AI response
   - CHAT-03: AI response references Karl's actual data
   - CHAT-04: Follow-up questions use prior context
   - CHAT-05: Chips visible on first open, disappear after first message
   - CHAT-06: Close button and × button both close the panel
   - CHAT-07: Loading dots appear while API call is in-flight
   - CHAT-08: Error bubble appears on failure
   - CHAT-09: Input cleared after send; empty input blocked
   - CHAT-10: Widget matches editorial monochrome design system (no jarring colors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-chat-widget/01-03-SUMMARY.md`
</output>
```
